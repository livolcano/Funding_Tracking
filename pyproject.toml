[project]
name = "funding-tracking-mvp"
version = "0.1.0"
description = ""
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4==4.14.2",
    "httpx==0.28.1",
    "openpyxl==3.1.5",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
